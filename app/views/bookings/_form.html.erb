<%= form_with url: update_bookings_path, method: :patch do |f| %>
<%= f.check_box booking.approved, checked: booking.approved,  onchange: "this.form.submit();"%>
<%= f.hidden_field :booking_id, value: booking.id %>
<% end %>