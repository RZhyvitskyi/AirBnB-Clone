<%= form_with model: booking, class: "d-flex flex-column align-items-start gap-2" do |f| %>
  <%= f.label demon.name %>
  <%= f.hidden_field :demon_id, value: demon.id %>
  <%= f.submit class: "btn btn-secondary" %>
<% end %>